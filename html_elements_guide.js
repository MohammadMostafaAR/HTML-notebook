window.addEventListener("DOMContentLoaded", () => {
    // const sidebar = document.getElementById('summary_list');
    // const initialTop = 50; // how far from top it starts
    // const maxOffset = 0; // stop when it reaches this

    // if (!sidebar) return;

    // sidebar.style.position = 'relative';
    // sidebar.style.top = `${initialTop}px`;

    // window.addEventListener('scroll', () => {
    //     const scrollY = window.scrollY;
    //     const newTop = Math.max(initialTop - scrollY, maxOffset);
    //     sidebar.style.top = `${newTop}px`;
    //     sidebar.style.position = "fixed";
    // });
});

